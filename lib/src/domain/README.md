# Domain Layer

Owns application flows, use-facing service contracts, and business value objects. Domain code coordinates lower-layer abstractions but must not bind directly to concrete player engines, provider SDKs, databases, HTTP clients, or filesystem layouts.
