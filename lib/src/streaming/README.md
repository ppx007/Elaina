# Streaming Layer

Owns virtual media streams, BT streaming, piece priority, range serving, and buffered range tracking. Playback consumes Streaming abstractions rather than concrete download engines.
