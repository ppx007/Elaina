# UI Layer

Owns screens, widgets, and presentation state. UI code must depend on Domain-facing contracts and must not directly import MPV, VLC, Bangumi, Dandanplay, libtorrent, yuc.wiki, provider implementations, gateway internals, or storage internals.
