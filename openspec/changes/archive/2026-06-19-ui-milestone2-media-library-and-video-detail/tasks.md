## 1. Media Library Page Implementation

- [x] 1.1 Create `MediaLibraryPage` widget conforming to the `desktop-media-library` capability spec [UI Layer]
- [x] 1.2 Bind folder list and scanner trigger controls to [MediaLibraryRuntime] and display real-time scan progress [UI Layer]

## 2. Video Detail Page Implementation

- [x] 2.1 Create `VideoDetailPage` widget conforming to the `desktop-video-detail` capability spec [UI Layer]
- [x] 2.2 Implement the episode selection grid and hook the "Continue Playback" shortcut button to the playback screen routing [UI Layer]

## 3. Verification and Integration Testing

- [x] 3.1 Write UI widget and driver tests for library scanning and video details list interactions [UI Layer]
- [x] 3.2 Run repository analysis and layer boundary checkers to ensure zero forbidden concrete backend imports [UI Layer]
